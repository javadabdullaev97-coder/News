# Bild notes: school-security-requirements-425

## Стратегия: none → очередь редактора (reason `no-image`)

STATUS: fallback-needed
`image` оставлен с `null`-ами, во frontmatter выставлен `awaitingEditor: true`.

## Почему не A (первоисточник)

Все три `sources` — нормативные акты на lex.uz (№ 425, PDF, ПП-360). У акта иллюстрации
нет по определению, искать её там нечего. Поэтому отрабатывал профильные ведомства.

### Проверенные официальные каналы

| Канал | Статус | Что нашлось |
|---|---|---|
| `@uzedu` | официальный, 106K подписчиков, описание ссылается на uzedu.uz | 117 постов, 15.07–01.08 — ни одного про охрану школ |
| `@poytaxt_uz` | официальный, 25,8K, описание ссылается на tashkent.uz | 103 поста, 24.07–02.08 — ни одного про охрану школ |
| `@mvd_uz` | публичного превью нет (0 постов в `t.me/s/`) | — |
| `@mchs_uz` | публичного превью нет | — |

Метод: `curl https://t.me/s/<channel>` с постраничной проходкой через `?before=<id>` и
парсингом `background-image:url(...)` в блоках `data-post`. Перебрал по 12 страниц на канал.
Поиск по ключам: `xavfsiz`, `qo'riqlash`, `қўриқлаш`, `безопас`, `охран`, `пропуск`, `КПП`,
`turniket`, `видеокузат`, `darvoza`, `425`, `o'quv yili`.

Дополнительно перебрал 26 вероятных хендлов МВД / МЧС / Департамента охраны
(`iiv_uz`, `mvduz`, `iichm_uz`, `iiv_matbuot`, `mvd_press`, `ichkiishlar`, `mvd_uzbekistan`,
`qorikhona`, `qoriqlash_uz`, `fvv_uz`, `favqulodda`, `mchs_uzb`, `edu_uz`, `toshkentedu`,
`gov_uz` и др.) — публичных превью с постами нет ни у одного.

Проверил `gov.uz/ru/news` и `uzedu.uz/ru/news` напрямую: списки новостей рендерятся на JS,
статического HTML с материалом по № 425 нет.

## Отклонённые варианты

- **`@qoriqlash` («Qo'riqlash | Rasmiy kanal»)** — на вид тот самый Департамент охраны,
  на деле **15 подписчиков**, 20 фото, подтверждения принадлежности к МВД нет. За официальный
  канал ведомства не считаю: атрибуция «Пресс-служба Департамента охраны при МВД» была бы
  недостоверной. То же с `@davlat_xavfsizlik` («POLITSIYA | Rasmiy kanal») — счётчиков нет,
  описание пустое.
- **`@uzedu/44402`** «Yangi o'quv yiliga tayyorgarlik ishlari» (подготовка школ Китабского
  района к учебному году) — тематически ближайшее, что нашлось. Пост видео, публично отдаётся
  только превью **320×180**. Это ниже `MIN_SOURCE = 600` в `prepare-image.py` → `status:
  too-small`, публиковать нельзя.
- **`@uzedu/44566`, `44573`, `44288`, `44309`** — выезды министра в школы Самарканда и
  Андижана. Живые фото, но это репортаж о встречах министра, а не об охране; плюс люди
  крупным планом на политически окрашенном материале.
- **`@uzedu/44561`** — карточка «Qabul 2026» про электронный приём в 1-й класс. Уже
  использована в другом материале выпуска и не о безопасности.
- **CC0-сток, Openverse API** (`api.openverse.org/v1/images`, фильтр `license=cc0,pdm`).
  Запросы: `school gate`, `school entrance`, `school fence`, `schoolyard`, `turnstile
  entrance`, `school building entrance`, `Uzbekistan school`, `Tashkent school`,
  `Uzbekistan classroom`, `Uzbek school children`, `Uzbekistan education`. Итог:
  - по Узбекистану выдача — школьницы крупным планом (`Samarkand, school girls`,
    `School Kids`, `Speak out!`). **Узнаваемые лица детей — стоп по поручению и по §8.**
    Остальное — медресе Регистана, к теме отношения не имеет;
  - географически нейтральных школьных ворот в CC0 нет: всё, что годится по кадру, — США,
    Япония, Китай, Финляндия, Британия, часто с читаемой вывеской школы
    (`Thorpedene Primary School's smoke-free school gates`, `Heinätori School Fence Oulu`).
    Ставить британские ворота к новости про узбекские школы — подмена географии, читатель
    прочтёт кадр как снимок с места;
  - большая часть остальной CC0-выдачи — `wikimedia`, а Commons мы не берём (лицензии
    требуют атрибуции конкретному автору, автоматически не валидируется).

## Почему не C (AI-генерация)

Инструментов Higgsfield в этом прогоне нет — MCP-тулов генерации в наборе не оказалось,
в репозитории интеграции тоже нет (`higgsfield` встречается только в текстах `.claude/agents/bild.md`
и чужих bild-notes). Так что вариант C физически недоступен, а не отвергнут по существу.

Отдельно замечу на будущее: тема здесь не абстрактная («ставка», «инфляция»), а конкретная
физическая сцена — вход в школу. Фотореалистичная генерация «школьный КПП в Ташкенте»
изобразила бы несуществующее место в документальной стилистике под новостью о реальных
школах. Даже с честным `credit: "AI generated (LEAP News)"` это ближе к риску, чем к пользе.

## Что отправлено владельцу

```bash
node scripts/editor-queue.mjs push \
  --slug=school-security-requirements-425 \
  --title="Кабмин утвердил правила охраны школ и вузов: родителей пустят по разрешению администрации" \
  --reason=no-image \
  --question="Живой фотографии по теме нет. ..."
```

**Команда в этом окружении не доставлена**: `editor-queue.mjs` требует `TELEGRAM_BOT_TOKEN`
и `TELEGRAM_EDITOR_CHAT_ID`, в прогоне их нет — скрипт вышел с сообщением
«Нужны TELEGRAM_BOT_TOKEN и TELEGRAM_EDITOR_CHAT_ID». Под `DRY_RUN=1` та же команда
отрабатывает штатно, формулировка проверена. Запись в `content/state/editor-queue.json`
не появилась: очередь хранит `messageId` ответного сообщения, без реальной отправки его нет.
**Команду нужно повторить в окружении с токенами** — иначе владелец вопроса не увидит,
а материал так и останется заблокированным.

Блокировка при этом уже стоит и от токенов не зависит: `scripts/build-posts.mjs` даёт
`⏸ ждёт ответа владельца — на сайт не выпущен`, в `lib/generated-posts.ts` статьи нет.
Ни на сайт, ни в Telegram она выйти не может.

## Что нужно от владельца

Фото школьного входа, поста охраны, турникета или школьной ограды в Узбекистане —
общим планом, без узнаваемых детских лиц. Присланный кадр workflow положит в
`.review/editor-photo-school-security-requirements-425.jpg` и прогонит через
`prepare-image.py`. Альтернатива — «ок» на выпуск без картинки.

## Alt-текст
Не составлен — картинки нет.

## Уверенность в решении: 85%
Что первоисточник и доступный CC0-сток пусты — проверено перебором, а не на глаз.
Остаток неуверенности — в том, что часть ведомственных каналов (МВД, МЧС, Департамент охраны)
закрыта для публичного превью: там фото поста охраны в школе вполне может быть, но достать
его без Bot API или логина нельзя.
