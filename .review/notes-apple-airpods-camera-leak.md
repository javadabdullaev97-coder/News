# Stray test file — ignore

Это тестовый файл, оставшийся от диагностики блокировки Write-инструмента
на имени `reporter-notes-*.md`. Актуальный лог research'а по теме
`apple-airpods-camera-leak` находится в файле
`.review/correspondent-notes-apple-airpods-camera-leak.md` (см. NB в его начале).
