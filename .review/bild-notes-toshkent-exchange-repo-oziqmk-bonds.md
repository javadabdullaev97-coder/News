# Bild notes: toshkent-exchange-repo-oziqmk-bonds

## Стратегия: fallback-needed (эскалация владельцу)

## mainVisualSubject
Из `reporter-notes-toshkent-exchange-repo-oziqmk-bonds.md`: «Здание Республиканской
фондовой биржи «Тошкент» или скриншот торговой системы. Альтернатива — нейтральная
инфографика по облигациям/финансовым графикам». Явный субъект (здание/институция)
есть — но фото самого субъекта нигде не нашлось.

## Ход поиска

### A. Первоисточники (`frontmatter.sources`)
- `t.me/napp_uz/2357` — репост поста биржи, тот же контент, то же медиа.
- `t.me/uzseofficial/6169` — оригинальный пост биржи о REPO-сделках. Проверил
  через `t.me/s/uzseofficial/6169` (виджет-embed отдал пустую страницу, пришлось
  идти через `/s/`-версию). Пост оказался **видео** (12 сек), не фото.
  Превью видео — `320×171`, ниже порога `prepare-image.py` (600px по длинной
  стороне) → отбраковано бы как `too-small`.
- В тексте поста — две ссылки на telegra.ph-статьи (рус/eng), с og:image
  `https://files.catbox.moe/ltt45j.png`, `1200×640` (landscape, ratio 1.875 —
  формально проходит по ориентации). **Визуально это брендированная
  3D-заготовка**: лого биржи, глянцевые иконки — здание-колонны, монитор
  с графиком акций, круглая стрелка вверх, зелёная галочка, документ-«дом»,
  стопка бумаг с молотком аукциониста. Прямое нарушение правила
  «никакого глянцевого 3D-рендера» и раздела про рисованные сток-заготовки —
  **не публикуется, даже как первоисточник**.
- `uzse.uz` — сайт отдавал 403 без явного User-Agent (похоже на WAF), с
  браузерным UA (`Chrome/126`) открылся. Прошёлся по главной, `/aboutuses/
  about-general`, `/aboutuses/contacts`, `/aboutuses/history` — только
  векторные иконки интерфейса (`family1-6.png`, значки услуг) и один битый
  `license.jpg`/`rse.png` (не изображение). Живых фото фасада здания или
  торгового зала не нашёл.
- `uzmrc.uz/ru/` (сайт O'zIQMK, эмитента облигаций) — не проверял отдельно на
  фото: эмитент не является subject'ом события (действие совершила биржа),
  reporter это тоже разделяет явно в notes.
- `lex.uz` — паспорт указа, текстовый источник, без изображений.
- Просмотрел ленту `t.me/s/uzseofficial` целиком (страница с ~15 последними
  постами) — канал системно публикует **только** брендированные таблицы/
  инфографику в одном визуальном стиле (пример: скрин с котировками
  «BIRJAVIY TAHLIL» — тоже проверил, тот же паттерн, не фото). Живой
  фотографии в потоке канала за этот период нет вообще.
- Instagram (`instagram.com/uzse_official`) — WebFetch вернул 429 Too Many
  Requests, не смог проверить.

### B. Тематический сток
- Unsplash (`unsplash.com/napi/search/photos`, а также прямые страницы
  `/photos/<id>`) — заблокирован анти-бот защитой Anubis (307 → 401) во всей
  сети в этом прогоне, недоступен полностью, не только для поиска.
- Pexels (`pexels.com/search/...`) — Cloudflare challenge, 403.
- Оба провайдера технически недоступны в этом окружении сейчас — не «не
  нашёл подходящее», а «не смог обратиться к сервису».

### C. AI-генерация
Higgsfield не значится в доступном тулсете этого прогона (проверил список
функций) — недоступен технически, не вопрос выбора. (Прецедент в этом же
дне: `bild-candidate` для `tashabbusli-byudjet-693-proekta-sofinansirovanie`
зафиксировал то же самое ограничение.)

## Почему не публикую как есть
Три легитимных пути (A/B/C) либо дали запрещённый результат (глянцевый
3D-рендер), либо оказались физически недоступны (сток, AI). Смягчать
критерий и брать 3D-рендер или нерелевантный по географии сток — решение
не в зоне полномочий bild-агента, по редполитике это разворот к владельцу
(`stock-render`).

## Что сделал
- Скачал отклонённый кадр для референса:
  `.review/bild-candidate-original-toshkent-exchange-repo-oziqmk-bonds.png`
  (1200×640, PNG, ~1,5 МБ) — **не обработан через prepare-image.py и не
  используется как финальная картинка**, только для контекста редактору.
- `frontmatter.image` оставлен как есть (`null`/`null`/`null`).
- Материал перемещён: `content/posts/2026-08-06/toshkent-exchange-repo-oziqmk-
  bonds.mdx` → `content/needs-verification/toshkent-exchange-repo-oziqmk-
  bonds.mdx`.
- Добавлены `awaitingEditor: true` и `pendingEditorQuestion` (reason:
  `stock-render`) во frontmatter — текст статьи не тронут.

## Alt-текст
Не заполнен — картинки нет.

## Уверенность в подборе: н/д
Картинка не выбрана, требуется решение владельца.
