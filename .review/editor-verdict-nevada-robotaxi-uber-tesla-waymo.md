# Editor verdict: nevada-robotaxi-uber-tesla-waymo

## Решение: killed

## Причина: дубль темы, уже стоящей в очереди на публикацию — не проблема качества

Fact-check вернул `approve-with-downgrades` (confidence 60%) с корректным и
выполнимым списком из 9 понижений (атрибуция «по данным Engadget» в заголовке
и лиде, разведение задокументированного интерим-ордера NTA от 27 июля и
неподтверждённых NTA-текстом цифр 5000/1000/1000). Само по себе это не повод
для `killed` — понижения были готовы к механическому применению.

Причина решения другая. Перед редактированием я проверил `content/queue/` и
`.review/` на совпадения по теме (округ Кларк, NTA, Tesla/Uber/Waymo,
20 августа) и обнаружил: тот же факт — одобрение NTA от 20 августа флота
Tesla (5000)/Waymo (1000)/Uber (1000), плюс тот же задокументированный
интерим-ордер на 10 машин от 27 июля по Docket 26-05015 — уже прошёл полный
конвейер под слагом `nevada-robotaxis-tesla-uber-waymo` и лежит в
`content/queue/nevada-robotaxis-tesla-uber-waymo.mdx` полностью готовым к
выходу: `editor-verdict` там — `publish` (92% готовности), уже проставлены
`tgScore: 35`, `broadcast: false`, `mainChannelGate: regulatorDecision`,
изображение и `publishedNotice` (`low-confidence`, source TechCrunch).

Более того, это уже вторая коллизия по этому сюжету за 21 августа: до этого
`tesla-robotaxi-nevada-expansion` была снята в пользу того же
`nevada-robotaxis-tesla-uber-waymo` (см.
`.review/collision-note-tesla-robotaxi-nevada-expansion.md`). Мой черновик —
третий независимый заход на одно и то же событие. Публикация ещё одной статьи
об одном и том же одобрении NTA создала бы дублирующий материал на сайте и в
рассылке — редакция уже приняла решение не дублировать публикацию в
прецедентном случае, применяю тот же принцип здесь, до выхода в очередь, а не
после.

Полное обоснование коллизии — `.review/collision-note-nevada-robotaxi-uber-tesla-waymo.md`.

## Что НЕ делалось и почему

Стилистическая доводка (9 понижений fact-checker'а, заголовок, лид, тело,
frontmatter) не выполнялась — она не имеет смысла для материала, который не
идёт в публикацию. Черновик перенесён в `content/rejected/` в исходном виде
(тот же текст, что получен от reporter'а), чтобы сохранить полный контекст
для разбора: понижения fact-checker'а видны в `FACT_CHECK_PATH`, применять их
задним числом не требуется.

## Проверка трёх жёстких триггеров возврата к reporter'у

Не применимо — решение `killed` принято до начала редактирования текста,
ни один абзац, ссылка или запись `sources` не менялись. Триггеры не
срабатывали, потому что редактирования не было.

## Что оставил как есть

Весь черновик — без изменений, перенесён как есть в `content/rejected/`.

## Метаданные

- Финальная длина: не считалась (материал не публикуется)
- Категория: tech (для справки, как была у reporter'а)
- Теги: не пересматривались

## Уровень готовности к публикации: 0% (killed — дубль темы)
