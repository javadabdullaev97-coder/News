# Bild notes: cbu-biometric-fintech-relax

## Стратегия: stock (фототека редакции, B0)

## Ход подбора
1. Проверил `mainVisualSubject` в reporter-notes: явного субъекта нет —
   тема про изменение цифровой процедуры регистрации в финтех-приложениях.
   Reporter и handoff (`image-hint`) предлагают либо экран смартфона с
   OTP/формой входа, либо здание/вывеска ЦБ РУз.
2. Первоисточник (Strategy A): reporter уже проверил cbu.uz/press_center и
   @centralbankuzbekistan — официального анонса/фото по этим поправкам нет.
   Постановление опубликовано только на lex.uz как скан PDF с QR-подписями —
   не фотография, использовать нельзя.
3. Тема попадает в категорию "официального фото не бывает в природе" —
   регуляторная поправка без пресс-конференции и без узнаваемого визуального
   события. По правилам B0 обязателен запрос к фототеке редакции, а не поиск
   на глаз.
4. Запросил `node scripts/pick-stock.mjs --topic banking` — вернул
   `cbu-building-01.jpg` (0 использований, "последний никогда" — начало
   ротации). Тот же результат по `--topic cbu`.
5. image-avoid (handoff) — не показывать логотип конкретного банка/сервиса
   как единственный узнаваемый элемент. Здание ЦБ этому не противоречит:
   это регулятор в целом, а не один финтех-бренд.

## Источник картинки
- Файл: /images/stock/cbu-building-01.jpg (из `config/stock-photos.json`)
- Тип: фото здания ЦБ РУз, "из открытых источников" (атрибуция как в
  фототеке — автор не уточнён)
- Уже кадрировано редакцией под 16:9, `prepare-image.py` не применялся и
  копирование в `posts/2026-08/` не требуется — ссылка идёт на общий
  файл `/images/stock/...`, как предписано инструкцией к фототеке.
- Ширина исходника 640px — принято скриптом (порог фототеки ниже, чем
  минимум для героя; `--hero` не использовал, материал не идёт первым
  на главной).

## Отклонённые варианты
- Скан постановления на lex.uz (PDF с QR-подписями) — не фотография,
  для читателя нечитаемо и не по правилам (документ, не живой визуал).
- Поиск живого фото экрана OTP-формы в открытых банках (Unsplash) не
  запускал: по правилам B0 фототека редакции проверяется раньше публичного
  стока, и она дала валидный релевантный результат — искать дальше не
  требовалось.
- AI-генерация (Higgsfield) не понадобилась — фототека закрыла потребность.

## Alt-текст
"Здание Центрального банка Республики Узбекистан в Ташкенте, гранитный
фасад с названием на английском и флаги у входа" (взят из
`config/stock-photos.json` без изменений).

## Уверенность в подборе: 75%
Картинка нейтральная и по теме (регулятор, чьё постановление меняет
процедуру), но не иллюстрирует сам предмет новости (смартфон/OTP) —
это ожидаемый компромисс для тем без визуального субъекта, прямо
разрешённый handoff'ом editor'а.
