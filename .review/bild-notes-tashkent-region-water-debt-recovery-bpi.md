# Bild notes: tashkent-region-water-debt-recovery-bpi

## Стратегия: primary-source (кадр требует подтверждения — crop-risky)

## mainVisualSubject (из reporter-notes)
Явного субъекта нет — тема делопроизводственная (взыскание долгов). Reporter предложил
варианты: здание/логотип Генпрокуратуры или нейтральное фото труб/счётчика воды.

## Порядок поиска
1. **Первоисточник A — Telegram-канал БПИ (`t.me/mib_faoliyatidan/18576`).** Пост с самим
   объявлением (1 августа) содержит одно фото 800×600, взято через embed-версию
   (`t.me/mib_faoliyatidan/18576?embed=1` → `tgme_widget_message_photo_wrap`, cdn4.telesco.pe).
   На фото — сотрудник БПИ в форменной одежде за столом с флагом Узбекистана принимает
   документ у молодого посетителя, типичный рабочий кадр кабинета ведомства. Не постановка
   специально под водную тему, но это единственная живая фотография от самого БПИ,
   привязанная к этой публикации — предпочтительнее абстрактных труб/счётчика, так как
   показывает реальную работу ведомства, а не сток.
2. **Первоисточник B — Telegram-канал Генпрокуратуры (`t.me/uzbprokuratura/87480`).**
   Проверен через embed — фото к посту нет, только эмодзи-иконка в разметке.
   Отклонён: нет изображения.
3. Сайт `mib.uz` не проверялся — reporter-notes уже зафиксировали HTTP 503 при двух попытках
   (сайт недоступен), пере-проверка не имела смысла.
4. Не переходил к стоку/AI-генерации — фото из первоисточника A прошло все проверки
   `preferRealPhotography` (живая фотография, не рисованная заготовка) и `subjectFirst`
   (насколько это возможно для темы без явного субъекта — показывает работу самого БПИ).

## Обработка
`python3 scripts/prepare-image.py /tmp/mib_photo.jpg tashkent-region-water-debt-recovery-bpi --month=2026-08`

```json
{
  "source": {"width": 800, "height": 600},
  "fit": {
    "mode": "cover",
    "scale": 2.0,
    "cropLoss": 0.25,
    "offset": [0, 139],
    "upscaled": true,
    "note": "обрезано 25% по высоте; окно кропа выбрано по содержимому; исходник растянут в 2.00× ради заливки кадра"
  },
  "output": {
    "path": "public/images/posts/2026-08/tashkent-region-water-debt-recovery-bpi-01.jpg",
    "width": 1600, "height": 900, "bytes": 177515, "quality": 90
  },
  "status": "ok"
}
```

Исходник 800×600 (4:3) заливает кадр 16:9 с обрезкой 25% по высоте — выше порога
`cropRiskThreshold: 0.15` из `config/newsroom-policy.json`. На фото — два человека
(явный объект по политике эскалации), поэтому механический порог сработал, несмотря
на то, что визуальная проверка итогового кадра (`public/images/posts/2026-08/
tashkent-region-water-debt-recovery-bpi-01.jpg`) показала: оба лица и руки целиком
в кадре, обрезаны только полоса потолка сверху и край стола снизу — ничего значимого
не потеряно. Решил не публиковать напрямую и эскалировать по правилу (аналогичный
прецедент уже был в очереди: `uzbekistan-usd-rate-rising-aug2026`, cropLoss 0.156).

## Отклонённые варианты
- Пост Генпрокуратуры (`uzbprokuratura/87480`) — без изображения (только эмодзи в разметке).
- Абстрактные варианты (труб/счётчика воды, здание Генпрокуратуры) не понадобились —
  нашлось живое официальное фото самого БПИ; к абстрактным вариантам вернусь, если владелец
  попросит другой кадр в ответ на вопрос в очереди.

## Alt-текст
«Сотрудник Бюро принудительного исполнения в форменной одежде за рабочим столом с флагом
Узбекистана принимает документ от посетителя в кабинете ведомства»

## Эскалация
Материал перемещён в `content/needs-verification/tashkent-region-water-debt-recovery-bpi.mdx`
с `awaitingEditor: true` и `pendingEditorQuestion.reason: "crop-risky"`. Вызвать
`node scripts/editor-queue.mjs push` напрямую не могу — нет доступа к TELEGRAM_BOT_TOKEN
(секрет только внутри GitHub Actions). Дальше материал подхватит `scan-pending` в рамках
workflow `editor-queue.yml` и отправит вопрос владельцу.

## Уверенность в подборе: 55%
Фото официальное, живое, по теме ведомства — сильная сторона. Но кроп выше порога на фото
с людьми и отсутствие прямой визуальной связи с водной тематикой (это общий рабочий кадр
БПИ, не съёмка именно взыскания долгов за воду) удерживают от более высокой оценки.
