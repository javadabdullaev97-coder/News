# Bild notes: personalnye-dannye-adekvatnye-strany-pkm-415

## Стратегия: stock

## Почему не A (первоисточник)
- `lex.uz/ru/docs/8369688` — проверил через WebFetch: страница постановления
  чисто текстовая (таблица списка стран), `og:image` отсутствует, в контенте
  только служебные иконки/логотип lex.uz — не пригодны.
- `@mvd_uz` (P1, `config/telegram-channels.json`) — проверил ленту канала
  (`t.me/s/mvd_uz`) на предмет поста о постановлении №415 / перечне стран —
  такого поста нет; сам список стран готовит Департамент миграции и
  персонализации при МВД, у которого нет отдельного канала/сайта в
  `config/telegram-channels.json` и `config/news-sources.json` (проверил grep).
- Явного `mainVisualSubject` от reporter'а нет (harness заблокировал notes-файл,
  задание описывает это прямо) — тема регуляторная без человека/здания-героя,
  попытка A закономерно не даёт результата.

## Источник картинки
- URL: https://unsplash.com/photos/a-black-and-white-photo-of-a-network-of-lines
  (фактически используемый файл: https://images.unsplash.com/photo-1558494949-ef010cbdcc31)
- Автор: Taylor Vick, Unsplash License (свободное использование, атрибуция
  указана из вежливости, не обязательна юридически)
- Тип: живая фотография серверной стойки с подсвеченными оптическими кабелями —
  не рисованный 3D-рендер, не «бизнес-концепция» на клавиатуре, не график/диаграмма
- Размер оригинала: 3000×1684 (альбомная ориентация, 1.78 — соответствует 16:9)
- prepare-image.py: cover, масштаб 0,5344, обрезано 0,21% по ширине (окно кропа
  по содержимому) → 1600×900, 371 КБ, quality 90, upscaled: false

## Промпт (AI-генерация)
Не использовалась — инструмент генерации изображений (Higgsfield) не был
доступен в списке инструментов этой сессии, несмотря на упоминание в системных
MCP-инструкциях. Пошёл по стратегии B (сток) вместо C.

## Отклонённые варианты
- Unsplash «a golden padlock sitting on top of a keyboard» (Towfiqu barbhuiya) —
  отклонил как штамп «бизнес-концепция на клавиатуре ноутбука», прямо запрещённый
  правилом №2 (живая фотография — да, но именно этот кадр входит в список клише
  редполитики)
- Unsplash «geometric shape digital wallpaper» (fabio) — абстрактная геометрия,
  не читается как «данные/защита», слишком декоративно
- Unsplash премиум-результаты (`plus.unsplash.com`, Getty Images) — платные,
  требуют Unsplash+ подписку, не CC0/Unsplash License — пропустил по правилу
  «не тащи Getty/платное»
- Unsplash «graphical user interface» / «computer coding screengrab» — экран
  кода крупным планом, менее релевантно теме трансграничной передачи данных,
  чем серверная инфраструктура
- Здание Департамента миграции и персонализации МВД — не нашёл ни одного
  открытого фото (ни на lex.uz, ни на mvd.uz, ни в Telegram МВД)

## Alt-текст
"Серверная стойка с подсвеченными сетевыми кабелями — символ трансграничной
передачи данных"

## Уверенность в подборе: 65%
Тема абстрактная регуляторная без явного subject — картинка честно иллюстрирует
«передачу данных/цифровую инфраструктуру», а не конкретное постановление или
ведомство. Это ожидаемый потолок для такой темы без AI-генерации в фирменной
палитре (инструмент был недоступен). Уверенность не выше, потому что кадр не
несёт узбекистанского контекста и не «subject-first» в смысле правила №1 — но
альтернативы (README рассказал про здание ведомства) физически не нашлось.
