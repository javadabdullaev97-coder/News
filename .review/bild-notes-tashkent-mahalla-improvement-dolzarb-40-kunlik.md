# Bild notes: tashkent-mahalla-improvement-dolzarb-40-kunlik

## Стратегия: fallback-needed (ушло на подтверждение владельцу)

## Что проверил

### A. Первоисточники (sources из frontmatter)
- `https://t.me/poytaxt_uz/51314` — пост хокимията Ташкента, пересказ сюжета «Узбекистон 24».
  Это видео-пост (11:23), не фотопост. Единственная доступная картинка — превью видео.
  Проверил через `t.me/s/poytaxt_uz/51314` (публичный виджет) и через og:image плоской
  страницы `t.me/poytaxt_uz/51314` — оба пути отдают один и тот же файл: **320×180 px**
  (13 214 байт). Ниже порога `prepare-image.py` (нужно ≥600px по длинной стороне) —
  скрипт вернёт `status: too-small`, не пытался прогонять.
- `https://t.me/uchtepahokimligi/58633` — пост хокимията Учтепинского района, тоже видео
  (0:47). Превью тем же способом — **320×180 px** (11 206 байт). Тот же результат.
- Просмотрел соседние посты канала `poytaxt_uz` (51305, 51306, 51309–51313, 51315) в поиске
  фотопоста на ту же тему — нашёл только нерелевантные: два «доброе утро, Ташкент» с видами
  города и поздравление строителей с профдиём. Не использовал — не тот сюжет, вводило бы
  читателя в заблуждение насчёт того, что показано.

### B. Тематический сток (Unsplash / Pexels)
- API-ключей (`UNSPLASH_ACCESS_KEY`, `PEXELS_API_KEY` и т.п.) в окружении нет.
- Прямой скрейпинг `unsplash.com/s/photos/...` заблокирован анти-бот защитой (Anubis,
  HTTP 401 "Making sure you're not a bot").
- `pexels.com/search/...` заблокирован Cloudflare-челленджем (HTTP 403).
- `api.openverse.org` отвечает без ключа, но результаты по «courtyard cleanup» — не по теме
  (монастырь в Непале и т.п.), да и Openverse не входит в список одобренных редполитикой
  банков (только Unsplash/Pexels) — не стал использовать.

### C. AI-генерация (Higgsfield)
- По handoff это согласованный fallback для темы («нейтральная иллюстрация хашара/
  благоустройства двора»). В этой сессии инструмент Higgsfield не подключён (недоступен в
  списке функций), вызвать не могу technically.

## Решение
Ни одна из трёх стратегий не довелась до публикуемого результата в этом прогоне — не по
причине "нет подходящей картинки в природе", а по чисто техническим ограничениям текущей
среды (маленький исходник у источника + недоступность стоковых API + недоступность
Higgsfield). Поэтому не стал гадать и подставлять случайный сток не по теме.

Материал перемещён: `content/drafts/2026-08-10/tashkent-mahalla-improvement-dolzarb-40-kunlik.mdx`
→ `content/needs-verification/tashkent-mahalla-improvement-dolzarb-40-kunlik.mdx`,
frontmatter получил `awaitingEditor: true` и `pendingEditorQuestion` (reason: `no-image`).
`editor-queue.yml` подхватит вопрос через `scan-pending` и уйдёт владельцу в TG.

## Отклонённые варианты
- Превью видео `poytaxt_uz/51314` (320×180) — too-small, апскейл запрещён правилами.
- Превью видео `uchtepahokimligi/58633` (320×180) — too-small, тот же лимит.
- Фото из соседних постов канала `poytaxt_uz` (доброе утро/профпраздник строителей) —
  не по теме, не использовал.
- Openverse-результаты по «courtyard cleanup» — нерелевантны географически и тематически,
  банк не входит в одобренный список.

## Alt-текст
Не заполнен — картинки нет.

## Уверенность в подборе: 0% (материал не подобран, ушёл на подтверждение)
