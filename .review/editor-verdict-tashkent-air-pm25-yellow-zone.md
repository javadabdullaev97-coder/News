# Editor verdict: tashkent-air-pm25-yellow-zone

## Решение: publish

## Изменения в заголовке
**До**: «Воздух в Ташкенте вошёл в «жёлтую» зону — PM2,5 в 3,4 раза выше нормы ВОЗ»
**После**: без изменений.
Обоснование: 74 символа (60–90 ок), «что случилось + где» в первых 40 символах,
активный залог, число цифрой, без CAPS/«!»/оценочных прилагательных — правкам
не подлежал.

## Изменения в лиде
**До**: без изменений.
**После**: без изменений.
Обоснование: 30 слов (верхняя граница диапазона 25–30, но не превышена), одно
предложение, отвечает на что/кто/когда/где, дата есть, источник атрибутирован
(«сообщает IQAir»). Да, лид пересекается по содержанию с заголовком (PM2,5,
3,4 раза, ВОЗ) — но расширяет его конкретными AQI=66 и атрибуцией к IQAir,
это не дословный повтор. Решил не резать: сокращение забрало бы либо дату,
либо атрибуцию, оба элемента обязательны по редполитике.

## Применённые понижения fact-checker'а (механически, без повторной проверки)
1. **Frontmatter**: убрал `publishedAt` и `updatedAt` полностью — на этапе
   очереди эти поля не проставляются, их ставит публикатор при выходе
   материала (явное указание fact-checker'а/владельца от 04.08.2026).
2. **Абзац про расхождение IQAir/Узгидромет по PM2,5**: заменил формулировку
   «связано с» на «вероятно, объясняется» + добавил уточнение из downgrade-
   списка про сеть IQAir из 67 станций разных операторов (включая
   государственные и общественные датчики). Разбил на два предложения — было
   одно длинное (>35 слов), стало два по норме ритма.

## Прочие стилистические правки
- `description` переписал: старая версия («PM2,5 превысил среднегодовую норму
  ВОЗ в 3,4 раза, по данным IQAir и Узгидромета») ошибочно приписывала
  Узгидромету подтверждение превышения PM2,5 — по факту Узгидромет фиксирует
  превышение по PM10, а PM2,5 у него в пределах ПДК. Новая версия (141 символ)
  разделяет источники: «жёлтая зона по IQAir» / «Узгидромет фиксирует
  превышение по PM10» — точнее и не вводит в заблуждение в поисковой выдаче.
- `tags`: унифицировал «PM2.5» → «PM2,5» (в теле статьи везде запятая,
  русская типографика).
- `authors`: добавил `"editor"` к `"reporter"`.
- Тело статьи, заголовки H2, ссылки, цифры — не трогал: уже соответствуют
  редполитике (ёлочки, тире, разделители, атрибуция «сообщает»/«опубликовал»,
  никакого AI-slop и канцелярита не найдено).

## Что оставил как есть
- Раздел «Два разных стандарта — не противоречие» — можно было сократить, но
  он несёт полезный читателю контекст (почему цифры не спорят друг с другом),
  оставил без изменений.
- Абзац про историю превышений PM10 за три дня — фактическая база проверена
  fact-checker'ом, трогать источники не стал.
- `image` в frontmatter оставил с `null` — заполнение работа `bild`.

## Проверка триггеров возврата к reporter'у
- Удалено слов от тела статьи: 0 (только вставки/замены формулировок из
  downgrade-списка, объём текста вырос, а не сократился).
- Абзацев с сылкой на источник снято по своей инициативе: 0.
- Новых inline-ссылок или новых записей `frontmatter.sources` не добавлено: 0.
Ни один из трёх триггеров не сработал — возврат к reporter'у не требуется.

## Метаданные
- Финальная длина тела статьи: ≈430 слов
- Категория: society
- Теги: экология, качество воздуха, Ташкент, PM2,5

## Уровень готовности к публикации: 95%
