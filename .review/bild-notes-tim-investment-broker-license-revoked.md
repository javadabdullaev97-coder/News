# Bild notes: tim-investment-broker-license-revoked

## Стратегия: none → очередь редактора (reason `stock-render`)

STATUS: fallback-needed
`frontmatter.image` оставлен с `null`-ами, выставлен `awaitingEditor: true` +
`pendingEditorQuestion`. Материал перемещён из `content/posts/2026-08-06/` в
`content/needs-verification/` (по правилу — `scan-pending` ищет вопросы
только там и в `content/rework/`).

## mainVisualSubject (reporter notes)

Reporter прямо пишет: явного субъекта нет, предложение — «здание/логотип НАПП
либо нейтральная инфографика рынка ценных бумаг».

## A. Первоисточник — проверено, живой фотографии нет

- **Telegram-пост** `https://t.me/napp_uz/2358` — распарсил напрямую через
  `curl` (не полагался на AI-реконструкцию WebFetch — по предупреждению
  reporter'а в notes она ненадёжна для дословного текста). В HTML нашёл
  реальный post-photo (`tgme_widget_message_photo_wrap`, не аватар 160×160):
  `https://cdn4.telesco.pe/file/ogUNIP...jpg`, 800×554.
  Скачал и посмотрел: это брендированная карточка НАПП — сток-фото рук с
  папками документов и ручкой, тёмно-синяя градиентная плашка, логотип NAPP
  и заголовок **на узбекском языке** («Инвестиция воситачиси сифатидаги...
  лицензияси бекор қилинди»). Не документальная съёмка события/компании —
  типовой шаблон, который агентство переиспользует для разных объявлений
  о лицензиях (выдача/приостановка/аннулирование). Отклонил:
  1) текст на узбекском на обложке русскоязычной статьи — не наш формат;
  2) по духу правила `rejectStockRenders` — «Пресс-службы часто украшают
     релизы именно [декоративными заготовками]» — это ровно такой случай,
     просто не 3D-график, а декоративное стоковое фото с плашкой.
- **napp.uz — страница новости** (та же, что в `frontmatter.sources`,
  третья ссылка ведёт на эпизод 2024 года; проверил также актуальную новость
  2026 года по ссылке из поста, `.../investitsiya-vositachisi-sifatidagi-...`):
  открыл HTML, нашёл блок `news-center-img` с каруселью — **пустая**,
  `<div class="owl-carousel owl-theme"></div>` без единого слайда. Текст
  статьи подтверждён (совпадает с фактурой в драфте), картинки к статье нет.
- **napp.uz — главная и «О нас»**: только SVG-иконки и `logo.svg`, фото
  здания агентства на сайте нет вообще (проверено WebFetch + `curl`/`grep`
  по `<img src=`).
- **Реестр профучастников** (вторая ссылка в `sources`) — табличная
  страница, изображений не несёт по определению, не проверял отдельно.

## B. Тематический сток — проверено, тоже не подошло

Unsplash, поиск «stock exchange documents» (landscape) — топ-5 результатов:
люди за компьютерами в офисе, скриншот UI с графиком, макро документа с
цифрами, телефон с графиком котировок, «бизнесмен анализирует данные».
Все — типовые «business concept» клише того же рода, что запрещает
`rejectStockRenders` (просто не 3D-рендер, а фотографический эквивалент);
ни один не завязан на Узбекистан или конкретно рынок ценных бумаг РУз.
Не считаю это апгрейдом по сравнению с брендированной карточкой НАПП —
отклонил всю пятёрку, дальше не листал.

Отдельно проверил, есть ли официальные фото у Республиканской фондовой
биржи «Тошкент» (тематически смежная, но другая организация) — не нашёл
рабочий домен быстрым поиском, и решил не тянуть: название биржи
слишком похоже на название самой компании-фигуранта («Тошкент Инвестиция
Мажмуи»/«Тошкент» биржа) — риск, что читатель примет фото биржи за фото,
связанное с самой компанией или НАПП. Не стал форсировать.

## C. AI-генерация — недоступна технически

В инструментах этого прогона нет Higgsfield generate_image/generate_image_batch
(есть только Read/Edit/WebFetch/Bash/Grep/Glob) — тот же результат, что уже
зафиксирован в `.review/bild-notes-school-security-requirements-425.md` для
другого материала: «MCP-тулов генерации в наборе не оказалось». Вариант C
физически недоступен в этой сессии, не отвергнут по существу. Тема (лицензия
регулятора рынка ценных бумаг) как раз подходящая для AI-генерации в
фирменной палитре (`config/newsroom-policy.json → brandPalette`) — это
стоило бы сделать в прогоне, где инструмент есть.

## Что отправлено в очередь

```yaml
awaitingEditor: true
pendingEditorQuestion:
  reason: "stock-render"
  question: "Живой фотографии по теме нет: у napp.uz карусель изображений
    новости пустая, страница агентства без фото здания. Единственный визуал
    из первоисточника — брендированная карточка НАПП в Telegram-посте
    (сток-фото рук с документами и ручкой, поверх — логотип НАПП и заголовок
    на узбекском языке). Это декоративная пресс-заготовка, не документальная
    съёмка события, плюс текст на узбекском на обложке русскоязычной статьи.
    AI-генерация в этом прогоне недоступна (нет инструмента). Опубликовать с
    этой карточкой, прислать другое фото или выпустить без картинки?"
  image: null
```

Файл статьи перемещён:
`content/posts/2026-08-06/tim-investment-broker-license-revoked.mdx` →
`content/needs-verification/tim-investment-broker-license-revoked.mdx`.

Команда `node scripts/editor-queue.mjs push ...` в этой сессии не запускалась
напрямую — она требует `TELEGRAM_BOT_TOKEN`/`TELEGRAM_EDITOR_CHAT_ID`,
которых у меня нет (доступны только в GitHub Actions). Доставка вопроса
владельцу — через workflow `editor-queue.yml` (`scan-pending` каждые 10 мин),
он найдёт материал в `content/needs-verification/` по `awaitingEditor: true`.

## Отклонённые варианты (сводка)

- Telegram-карточка НАПП (`cdn4.telesco.pe/.../ogUNIP...jpg`, 800×554) —
  декоративная заготовка + текст на узбекском, не документальное фото.
- Unsplash «stock exchange documents» топ-5 — типовые business-concept
  клише, не привязаны к Узбекистану/теме.
- Республиканская фондовая биржа «Тошкент» — не нашёл рабочий сайт быстрым
  поиском; риск путаницы названий с фигурантом статьи, не форсировал.
- AI-генерация (Higgsfield) — недоступна технически в этом прогоне.

## Alt-текст
Не составлен — картинки нет.

## Уверенность в решении: 75%
Первоисточник (napp.uz + Telegram) проверен по существу, не на глаз: карусель
изображений новости пуста, пост несёт только декоративную заготовку с
узбекским текстом. Основная неопределённость — не пробовал более широкий
и целенаправленный поиск (Pexels, другие ключевые слова на англ./рус.,
здание НАПП через image-поиск) из-за отсутствия выделенного инструмента
поиска изображений в этой сессии; решение полагается на WebFetch-эвристику.
