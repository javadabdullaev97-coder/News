# Bild notes: uzbekistan-flood-mudslide-warning-aug4-7

## Стратегия: none → очередь редактора (reason `no-image`)

STATUS: fallback-needed
`frontmatter.image` оставлен с `null`-ами. Во frontmatter выставлен `awaitingEditor: true`
и `pendingEditorQuestion`. Файл перемещён из `content/posts/2026-08-04/` в
`content/needs-verification/uzbekistan-flood-mudslide-warning-aug4-7.mdx` (иначе
`scan-pending` его не найдёт).

## mainVisualSubject

Reporter-notes для этого слага в `.review/` не существует (`reporter-notes-uzbekistan-flood-mudslide-warning-aug4-7.md`
не найден — проверено `ls .review/`). Подсказку взял из задания: «горная река/сель в
предгорьях после дождя, либо официальная карта зон риска». Явного человека-субъекта нет —
это метеопредупреждение Узгидромета, а не отчёт о случившемся ЧП (сели ещё не произошли,
период риска 4–7 августа).

## Почему не A (первоисточник)

Оба источника в `frontmatter.sources` — посты `@uzgydromet` (проверено, что это официальный
канал через «g», не сквоттер `@uzhydromet` через «h», см. прецедент
`bild-notes-uzhydromet-wind-dust-storm-warning.md`).

- `https://t.me/uzgydromet/21858` (экстренное предупреждение) → фото 723×800: брендированная
  заставка «TEZKOR GIDROMETEOROLOGIK AXBOROT» — золотая шестиконечная звезда с иконками
  погоды, логотип Facebook-репоста. Нулевая информативность, карты зон риска нет.
- `https://t.me/uzgydromet/21862` (прогноз на 4 августа) → фото 800×800: типовая заставка
  «OB-HAVO PROGNOZI / ПРОГНОЗ ПОГОДЫ» с логотипом O'ZGIDROMET и рядом мелких погодных
  иконок понизу. Та же заставка, что уже была отклонена в прошлом прогоне
  (`bild-notes-uzhydromet-wind-dust-storm-warning.md`, отклонённые варианты, пункт про
  `21847`) — «типовая заставка... нулевая информативность, одинакова у всех».

Проверил всю ленту `@uzgydromet` за 1–4 августа (20 последних постов, `t.me/s/uzgydromet`,
ID 21846–21865): помимо двух постов выше — карты автостанций мониторинга воздуха (не по
теме селей), недельный прогноз погоды `#hafta` (21856, картинка — сад с фруктами, тоже не
по теме), и снова две одинаковые брендовые заставки «OB-HAVO PROGNOZI» (21853, 21851).
Ни одной карты риска селей/паводков с реальными данными по районам нет ни в одном посте.

Дополнительно проверил вероятные хендлы МЧС/хокимиятов: `@fvv_uz`, `@mchs_uz`, `@off_fvv`,
`@fvvuzb`, `@toshkentviloyat`, `@tv_hokimiyat`, `@bostanliq`, `@ahangaran` — публичного
превью с постами нет ни у одного (`t.me/s/<handle>` отдаёт 0 постов / «Telegram: Contact»).
`@toshvil` («Toshkent viloyati») и `@bostonliq` («Toshkentliklar 24») отдают посты, но не
из белого списка `config/telegram-channels.json` (принадлежность не подтверждена) и без
актуальных постов про сели 3–4 августа — последние в ленте датированы июлем.

## Почему не B (сток)

Искал через Openverse API (`api.openverse.org`, `license=cc0,pdm`), как в прецеденте
`bild-notes-school-security-requirements-425.md`:

- `mountain river flash flood`, `mudslide mountain`, `debris flow mountain` — выдача:
  наводнение в лесу Джорджии, США (`ChattOconeeNF`, серия «Flash flood after July 21
  weather») — густой зелёный лиственный лес, визуально ничего общего с полупустынными
  предгорьями Тянь-Шаня/Чаткала (Бостанлыкский, Ахангаранский районы). Подмена
  географии — читатель прочтёт кадр как снимок с места, хотя это Аппалачи.
- `Kyrgyzstan mountain river`, `Tajikistan mountain river storm` — нашёл «Vakhsh river,
  Nurek» (CC0, Flickr, `lensnmatter`) и «The mighty Tian Shan Range» (PDM, Flickr) —
  географически уместные (Центральная Азия, тот же горный массив), но по содержанию не
  подходят: первое — спокойный закат на водохранилище без дождя, второе — ледники и
  бирюзовое озеро при ясном небе, вдобавок с водяным знаком `journeysonquest.com`
  (водяной знак — стоп-фактор, прецедент rawpixel в `bild-notes-school-security...md`).
  Оба создают обратное впечатление: ясная спокойная погода, а не риск ливня и селя.
- `flood water road`, `storm rain mountain village`, `muddy river flood rocks` — выдача:
  исторические наводнения Брисбена 1893–1974 (архив), заповедники после урагана Сэнди
  (Мэриленд/Делавэр) — чужая, узнаваемая по архитектуре/подписям география и эпоха.
- `rain clouds mountain valley village`, `wadi flash flood`, `flooded stream desert
  canyon` — пусто (0 результатов).
- `storm rain mountain village` → «Rain on the valley» — деревья Джошуа (Мохаве, США/
  Калифорния) — крайне узнаваемое растение, худший вариант подмены географии.

Итог: ни один сток не проходит одновременно и по географии (не превращается в подмену
места), и по содержанию (гроза/паводок, а не ясная погода), и без водяных знаков.

## Почему не C (AI-генерация)

Инструментов Higgsfield в наборе тулов этого прогона нет: вызов `generate_image` вернул
`Error: No such tool available`. Отдельно проверил `scripts/editor-queue.mjs` (тоже нужен
для очереди) — падает с `ERR_MODULE_NOT_FOUND: yaml` даже при простановке дефолтных
переменных окружения, то есть окружение в этом прогоне не укомплектовано npm-зависимостями для этого звена
автоматизации. Вариант C физически недоступен, а не отвергнут по существу —
как и в прецеденте `bild-notes-school-security-requirements-425.md`.

Отдельно по существу (на будущее, если тул появится): тема здесь не абстрактная
(«ставка», «инфляция»), а конкретный физический прогноз для конкретных районов. Генерация
«горная река во время дождя в Ташкентской области» рисовала бы несуществующий, но похожий
на документальный кадр под новостью о реальном предупреждении для реальных людей — риск
того же типа, что и с фотореалистичным стоком, просто более управляемый (можно явно
избежать похожести на конкретное место). Это было бы приемлемо при честном
`credit: "AI generated (LEAP News)"`, но сейчас неприменимо технически.

## Что отправлено владельцу

```bash
node scripts/editor-queue.mjs push \
  --slug=uzbekistan-flood-mudslide-warning-aug4-7 \
  --title="Узгидромет предупредил о риске селей и подтоплений в 4 областях 4–7 августа" \
  --reason=no-image \
  --question="Живой фотографии/официальной карты риска нет ни у Узгидромета (только
    брендированные заставки-шаблоны без данных), ни в геосовместимом CC0-стоке (Vakhsh/
    Tian Shan — без дождя/селя, США-фото — чужая природа). AI-генерация недоступна в этом
    прогоне (нет Higgsfield-тулов). Прислать фото гор/реки в Ташкентской области или
    подтвердить публикацию без картинки?"
```

**Команда в этом окружении не доставлена**: падает на `ERR_MODULE_NOT_FOUND: yaml` в
`lib/frontmatter.mjs`, ещё до проверки `TELEGRAM_BOT_TOKEN`/`TELEGRAM_EDITOR_CHAT_ID`.
Нужно повторить команду в окружении с установленными npm-зависимостями и токенами —
иначе владелец вопроса не увидит. Блокировка публикации при этом уже стоит независимо от
токенов: файл лежит в `content/needs-verification/` с `awaitingEditor: true`, поэтому
`scripts/build-posts.mjs` (по аналогии с прецедентом) должен показать материал как
ожидающий ответа владельца, а не выпустить на сайт.

## Что нужно от владельца

Фото гор/реки/дождя в Ташкентской, Наманганской, Ферганской или Андижанской области
(Бостанлыкский, Ахангаранский районы и т.п.) — общим планом, без узнаваемых лиц.
Присланный кадр workflow положит в `.review/editor-photo-uzbekistan-flood-mudslide-warning-aug4-7.jpg`
и прогонит через `prepare-image.py`. Альтернатива — «ок» на выпуск без картинки.

## Alt-текст
Не составлен — картинки нет.

## Уверенность в решении: 80%
Первоисточник и CC0-сток проверены исчерпывающе (вся лента Узгидромета за 4 дня, 8
профильных хендлов МЧС/хокимиятов, 12+ поисковых запросов в Openverse). Основная
неопределённость — в недоступности AI-генерации в этом конкретном прогоне: с рабочим
Higgsfield-тулом решение вероятно было бы `ai-generated`, а не `fallback-needed`.
