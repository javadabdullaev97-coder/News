# Bild notes: animal-cruelty-penalty-law-2026

## Стратегия: editor-supplied (владелец прислал фото через editor-queue)

## Контекст
Материал уже был в `content/state/missing-images.json` (баг прогона 12:40 —
ворота не требовали картинки). Однако при разборе выяснилось, что картинка
уже физически на месте: `public/images/posts/2026-08/animal-cruelty-penalty-law-2026-01.jpg`
(1600×900, JPEG) — появилась не через обычный pipeline bild'а, а как побочный
эффект другого события.

Хронология (см. `git log` по файлу и `content/state/editor-queue.json`):
- Fact-check раунд 2 дал `needs-fix` (confidence 82%), `directPublish` не
  открылся из-за буквального требования `factCheckVerdict: approve`.
- Editor поставил материал в очередь с `reason: "low-confidence"`, вопрос
  ушёл владельцу в Telegram: «Публиковать как есть?».
- Владелец ответил не текстом, а **фото** (реплаем на вопрос) — по правилам
  бота любое фото-реплай становится главной картинкой материала
  (`scripts/editor-queue.mjs`, ветка `kind: "photo"`).
- Файл сохранён в `.review/editor-photo-animal-cruelty-penalty-law-2026.jpg`
  (800×491, JPEG, 54 КБ), прогнан через `prepare-image.py` автоматически
  внутри `editor-queue.mjs` → `animal-cruelty-penalty-law-2026-01.jpg`
  (1600×900, 153 КБ).
- `editor-queue.mjs` при применении фото-ответа проставил **generic
  placeholder** в frontmatter: `alt: "Фотография к материалу"`,
  `credit: "LEAP News"` (это захардкожено в скрипте, не через bild) —
  задача этого прогона была заменить placeholder на нормальные alt/credit.

## Что на фото
Портрет Саиды Мирзиёевой (руководитель Администрации президента Узбекистана)
на фоне флага Узбекистана, официальная обстановка (пресс-встреча/мероприятие).
Она — прямой субъект материала: единственная цитируемая персона в статье
(её пост в Telegram о штрафах за жестокость к животным). Правило
subject-first соблюдено — фото показывает именно того, о ком материал,
а не абстракцию.

Reporter в notes указывал `mainVisualSubject`: кошки Ялпиз и Дархан
(фрейм-грэб из видео Мирзиёевой) как первый вариант, портрет/здание
Администрации — как альтернатива «если кадр из чужого видео нежелателен
по авторским причинам». Кадр с кошками не брался (нет доступа к видео как
источнику кадра без риска по авторским правам), портрет самой Мирзиёевой —
допустимая и даже более прямая альтернатива по тому же правилу.

## Проверка на риски
- Это не AI-генерация и не безымянный сток — реальное фото реального
  человека, отобранное и присланное лично владельцом редакции (высшая
  инстанция в этом workflow), не automated-подбором. Ограничение «никаких
  политиков в AI-генерации» здесь неприменимо — это не генерация.
- Попытался найти точный первоисточник фото (аватар/посты канала
  `t.me/SShMirziyoyeva`, поиск) — не нашёл точного совпадения кадра.
  Происхождение фото за пределами факта «прислано владельцем» не подтверждено,
  поэтому credit сформулирован честно и без вымышленной атрибуции конкретной
  пресс-службе, которую не могу подтвердить.

## Обработка
Уже выполнена автоматически внутри `editor-queue.mjs` через
`prepare-image.py` в момент применения ответа владельца (commit `ee7fbf0`).
Пересчёт не делал — файл уже 1600×900, апскейла/переобрезки не требуется.
Итог по факту: 800×491 → 1600×900 (contain, т.к. исходник вертикально/
горизонтально не совпадает с целевым кадром и меньше 1600px по ширине).

## Правки этого прогона
Заменил в `frontmatter.image` только `alt` и `credit` (placeholder →
содержательные значения). `url` не менял, файл картинки не трогал.

- `alt`: "Саида Мирзиёева, руководитель Администрации президента Узбекистана,
  на официальном мероприятии на фоне флага Узбекистана"
- `credit`: "Предоставлено редакции LEAP News" (честно отражает: фото
  реальное, происхождение вне факта «прислано владельцем» не подтверждено;
  не использовал `credit: "LEAP News"` дословно, т.к. это по редполитике
  зарезервировано за собственной съёмкой редакции, а это не наш кадр)

Также убрал `content/posts/2026-08-03/animal-cruelty-penalty-law-2026.mdx`
из списка `content/state/missing-images.json` — картинка на месте, статья
больше не требует внимания в следующих прогонах.

## Уверенность в подборе: 75%
Subject-first соблюдён, фото живое и релевантное (прямая цитируемая персона
материала), утверждена человеком-владельцем. Снижение с обычных 90%+ —
только из-за неподтверждённого точного первоисточника фото (credit не может
сослаться на конкретную пресс-службу).
