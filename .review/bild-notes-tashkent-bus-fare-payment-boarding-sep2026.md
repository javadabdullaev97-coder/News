# Bild notes: tashkent-bus-fare-payment-boarding-sep2026

## Стратегия: none → editor-queue (`no-image`)

Картинка не прикреплена. `frontmatter.image` оставлен с `null`-ами, проставлены
`awaitingEditor: true` и `pendingEditorQuestion` (reason `no-image`). Файл уже находился
в `content/needs-verification/` (не в `content/posts/`), поэтому переносить его не потребовалось —
`scan-pending` подхватит `pendingEditorQuestion` прямо оттуда.

## mainVisualSubject (по reporter-notes)

«Пассажир прикладывает транспортную карту к валидатору при входе в городской автобус в Ташкенте
(или общий план посадки в автобус на остановке)». Явный, конкретный субъект — но не публичное лицо
и не здание, поэтому официальное/фото/сток теоретически допустимы вместе с людьми (не политическая
тема, риска deepfake нет).

## A. Первоисточник — что реально лежит в посте Минтранса

`t.me/Mintrans_uz/28602` (raw HTML embed, `?embed=1`):
- В посте нет фото-медиа, только видео 3:01 (не для скачивания и не по регламенту).
- Видео-превью (`tgme_widget_message_video_thumb`, `background-image`) — скачано:
  `320×180`, JPEG. Содержание: студийная заставка телепрограммы «Jarayon» (ведущая в зелёном
  жакете на фоне логотипа передачи и глобуса) — это заставка новостной программы, а не автобус
  и не валидатор. Отклонено по двум причинам: (1) не по теме — не показывает субъект статьи;
  (2) `too-small` — 320px по длинной стороне < порога `prepare-image.py` в 600px.
- Аватар канала (`tgme_widget_message_user_photo`) — логотип Минтранса, 160×160, тот же файл,
  что и превью — тоже `too-small` и не по теме.

Проверен также соседний пост `t.me/Mintrans_uz/28590` (презентация модернизации транспорта
6 городов региона) — media-группа из 5 фото, 800×533. Просмотрены два: (1) чиновник у экрана
с картой маршрута и презентацией — фото совещания/презентации, не про оплату проезда;
(2) совещание за столом с флагами Узбекистана — то же самое. Отклонены: другая тема (маршрутная
сеть областных городов, не оплата при входе в Ташкенте), не показывают автобус/валидатор/посадку.

Сайт `mintrans.uz` — SPA на Next.js, прямой запрос страницы отдаёт 404-заглушку через WebFetch;
og:image на `lex.uz/ru/docs/8255321` (текст постановления) отсутствует — страница чисто текстовая.

## B. Сток — что проверено и почему отклонено

- **Unsplash API / Pexels API** — ключей (`UNSPLASH_ACCESS_KEY`, `PEXELS_API_KEY`) в окружении нет,
  `401`. Прямой скрейпинг `pexels.com/search/...` — `403` (заблокировано без API).
- **Openverse API** (без ключа, license=`cc0,pdm`, где возможно `orientation=landscape`):
  запросы `bus card reader payment`, `contactless payment bus`, `public transport card validator`,
  `city bus Tashkent` — 0 результатов. `public transport card`, `bus boarding passengers`,
  `city bus`, `bus stop passengers` — результаты есть, но все не по теме: британские автобусы
  энтузиастов (Plymouth/Leicester/Cardiff), архивные ч/б фото американских вокзалов начала XX века
  (Library of Congress) — географически и хронологически не связаны с материалом.
  `tap card reader`, `contactless card reader`, `bus ticket machine` — все релевантные попадания
  либо на `upload.wikimedia.org` (запрещено политикой, см. ниже), либо не по сути:
  - `SumUp` (Flickr, PDM, 1024×683) — ручной банковский POS-терминал для розницы (книжный магазин
    на фоне), не автобусный валидатор — вводит в заблуждение о предмете статьи;
  - `Viby Torv, Midttrafik billetautomat` (Flickr, CC0, 1024×681) — уличный автомат по продаже
    билетов в Орхусе (Дания) с датскими надписями «BILLETTER/TICKETS» и брендом «Midttrafik» —
    не онбордовый валидатор, чужая страна и бренд в кадре, публиковать под нейтральным кредитом
    нельзя без объяснения, что это не Ташкент;
  - британские автобусы (`CCT 234z`, `OWE 271K East Lancs Bodied Bristol VR`) — не по географии
    и по типу транспорта (винтажные британские автобусы, не городская маршрутная сеть Ташкента).
  - **Прямое попадание** — `Tashkent bus card` (3000×4000, CC0) и `A Marshrutka in Tashkent`
    (876×657, CC0) — оба хостятся на `upload.wikimedia.org`. По редполитике Wikimedia исключена
    из источников («не бери с wikipedia.org» — лицензии требуют атрибуции конкретному автору,
    сложно валидировать автоматически). Не взято, несмотря на идеальное тематическое совпадение.

## C. AI-генерация — недоступна технически

Инструменты Higgsfield MCP (`generate_image`) не входят в тулсет бильд-редактора в этом прогоне
(доступны только Read/Edit/WebFetch/Bash/Grep/Glob) — та же ситуация, что и в
`bild-notes-tashkent-water-shutoff-aug-3.md` от того же дня. Заготовка промпта на случай, если
владелец подтвердит генерацию (бренд-палитра, без людей — валидатор не требует лица человека
в кадре):

«documentary editorial photograph, close-up of a contactless transit card validator mounted on a
handrail inside a modern city bus, a transit card held near the reader, blurred bus interior and
seats in the background, muted editorial palette, warm orange-red #FF4D2E accent, deep charcoal
and sand tones, no rainbow colours, no glossy 3D render, no people, no faces, no text, no logos,
soft natural daylight, 16:9»

## Отклонённые варианты (сводно)

- `t.me/Mintrans_uz/28602` — только видео 3:01, превью 320×180 (заставка «Jarayon», not-on-topic + too-small)
- Аватар `Mintrans_uz` — логотип 160×160, too-small
- `t.me/Mintrans_uz/28590` (2 из 5 фото просмотрено) — совещание/презентация, другая тема
- `mintrans.uz` — SPA, 404 через WebFetch без JS-рендера
- `lex.uz/ru/docs/8255321` — текстовая страница, og:image нет
- Unsplash/Pexels — нет API-ключей, прямой скрейпинг заблокирован (403)
- Openverse: `SumUp` POS-терминал, `Midttrafik` уличный автомат (Дания), британские автобусы
  (`CCT 234z`, `OWE 271K`) — не по теме/географии
- Openverse: `Tashkent bus card`, `A Marshrutka in Tashkent` — идеально по теме, но хостятся на
  `upload.wikimedia.org` — запрещённый источник по редполитике
- Higgsfield-генерация — инструмент недоступен в тулсете этого прогона (не отклонение по существу)

## Alt-текст

Не заполнен: картинки нет. Заготовка на случай присланного фото/генерации —
«Пассажир прикладывает транспортную карту к валидатору при входе в городской автобус
в Ташкенте» (уточнить по факту присланного кадра).

## Уверенность в подборе: н/д (материал в очереди)

Уверенность в решении не публиковать без картинки/не брать сомнительный сток — 85%. Единственное
содержательно точное совпадение (`Tashkent bus card`, Wikimedia) отклонено строго по букве
политики, а не по качеству кадра — это единственный пункт, где решение можно оспорить, поэтому
вопрос идёт владельцу.
