# Bild notes: tashkent-state-audit-bachelor-program

## Стратегия: fallback-needed (эскалация владельцу)

## Что проверено (порядок попыток A → B → C)

### A. Официальный источник
- `https://tsue.uz/` и конкретная новость с сайта ТГЭУ — сайт стабильно отдаёт
  `HTTP 502` (проверено 5 подряд запросов с интервалами). Wayback Machine —
  `archived_snapshots: {}`, снапшотов нет.
- `https://daom.tsue.uz/` (Высшая школа гос. аудита, приёмная форма) — рабочий
  сайт (200 OK), но на странице и в `main.css` только SVG-логотип, живых фото
  нет вообще.
- Telegram-пост `t.me/poytaxt_uz/51277` — есть вложенное фото, скачал:
  `180×320px` — меньше жёсткого минимума 600px по длинной стороне
  (`prepare-image.py` вернул бы `status: too-small`). К тому же вертикальное.
  Не использовал.
- `https://ach.gov.uz/ru/events-nav/news/detail/68c5666fff604a3ef1445ce1`
  (Счётная палата, источник в `sources`) — на странице реальные фото
  1280×720/853, но это кадры **другого события**: церемония присвоения
  почётного профессора Мураду Абдуллаеву в Нанкинском университете аудита,
  6 сентября 2025. Фото упомянуто в теле статьи фоновым фактом, но не
  основной темой (открытие бакалавриата в ТГЭУ). Все кадры несут водяной
  знак китайского медиа `荔枝新闻` (Lizhi News, Цзянсу) — права ach.gov.uz на
  них не подтверждены, атрибуция «Счётная палата» была бы неточной.
  → отклонено по `source-doubt` (права + несовпадение темы).
- `lex.uz/ru/docs/8201180` (постановление №248) — только служебные иконки
  сайта, фото нет.

### B. Тематический сток
- Unsplash, поиск `tashkent university` (landscape) — **0 результатов**.
- `source.unsplash.com` (быстрый прокси) — сервис недоступен (503, отдаёт
  HTML вместо изображения), устаревший эндпоинт.

### C. AI-генерация
- В этой сессии инструмент генерации изображений (Higgsfield) не
  предоставлен — недоступен для вызова, сгенерировать иллюстрацию не могу.

## Решение
Ни один из трёх путей не дал чистого результата: официального фото по
теме нет, сток пуст, генерация недоступна инструментально. Единственный
найденный кандидат (нанкинский кадр) — с сомнительными правами и
смещённым сюжетом. Публиковать вслепую нельзя (`stock-render`/`source-doubt`
эвристики существуют именно для таких случаев).

Материал перемещён `content/posts/2026-08-07/tashkent-state-audit-bachelor-program.mdx`
→ `content/needs-verification/tashkent-state-audit-bachelor-program.mdx`,
во frontmatter добавлено:
```yaml
awaitingEditor: true
pendingEditorQuestion:
  reason: "no-image"
  question: "..."
```
`editor-queue.yml` (сканирует `content/needs-verification/` и `content/rework/`)
доставит вопрос владельцу через TG в течение ближайшего цикла (~10 мин).

## Отклонённые варианты
- Фото из Telegram-поста Poytaxt.uz — 180×320px, ниже минимума 600px.
- Кадры церемонии в Нанкине с ach.gov.uz — водяной знак стороннего китайского
  СМИ (`荔枝新闻`), права не подтверждены; и другое событие (сентябрь 2025,
  не открытие бакалавриата).
- tsue.uz — недоступен (502) на протяжении всей проверки.

## Alt-текст
Не заполнен — `image.url/alt/credit` остаются `null` до ответа владельца.

## Уверенность в подборе: 0% (картинка не подобрана, эскалация)
